import mayflower.*;

public class Image extends Actor {
    
    // Constructor that sets the screen to img passed in
    public Image(MayflowerImage img) {
        setImage(img);
    }

    public void act() {
        
    }
}
